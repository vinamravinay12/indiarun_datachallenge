# Ranking step (rank.py) - CPU-only, no network. These are all it needs.
numpy==2.1.3
pandas==2.2.3

# Pre-computation only (precompute.py builds the cached embeddings offline).
# Not required to run rank.py if the artifacts are already present.
sentence-transformers==5.6.0
torch==2.12.1
